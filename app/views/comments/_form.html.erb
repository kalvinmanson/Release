<%= form_for(comment) do |f| %>
  <% if comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(comment.errors.count, "error") %> prohibited this comment from being saved:</h2>

      <ul>
      <% comment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <div class="form-group">
    <%= f.label :book_id %>
    <%= f.select(:book_id, Book.all.collect {|a| [a.name, a.id]}, { include_blank: 'None' }, { class: 'form-control' }) -%>
  </div>


  <div class="form-group">
    <%= f.label :post_id %>
    <%= f.select(:post_id, Post.all.collect {|a| [a.name, a.id]}, { include_blank: 'None' }, { class: 'form-control' }) -%>
  </div>

  <div class="form-group">
    <%= f.label :user_id %>
    <%= f.select(:user_id, User.all.collect {|a| [a.name, a.id]}, { }, { class: 'form-control' }) -%>
  </div>

  <div class="form-group">
    <%= f.label :parent_id %>
    <%= f.select(:parent_id, Comment.all.collect {|a| [a.id, a.id]}, { }, { class: 'form-control' }) -%>
  </div>

  <div class="form-group">
    <%= f.label :content %>
    <%= f.text_area :content, class: "form-control froalamin" %>
  </div>

  <div class="form-group">
    <%= f.submit "Save", class: "btn btn-primary" %>
  </div>
<% end %>
